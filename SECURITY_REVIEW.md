# تقرير المراجعة الأمنية (Security Review Report)

## 1. مشكلة تسريب الأسرار (Hardcoded Secrets / Environment Variables)
* **المكان:** ملفات الاتصال بقاعدة البيانات وإعدادات السيرفر (`server.js`, `database.js`).
* **الخطر المحتمل:** وجود رابط قاعدة البيانات (`DATABASE_URL`) والمفتاح السري للتوكن (`JWT_SECRET`) داخل الكود مباشرة قد يؤدي إلى تسريبها في حال وصول أي شخص لمستودع الأكواد (GitHub)، مما يمنحه تحكماً كاملاً بقاعدة البيانات وحسابات المستخدمين.
* **مستوى الخطورة:** 🔴 حرج (Critical).
* **طريقة المعالجة:** تم نقل جميع الأسرار إلى ملف `.env` مستقل، وتمت إضافة هذا الملف إلى `.gitignore` لمنع رفعه. كما تم إنشاء ملف `.env.example` كدليل للمطورين.

## 2. تخزين كلمات المرور كنصوص واضحة (Plaintext Passwords)
* **المكان:** المتحكم الخاص بالمستخدمين (`usersController.js` أو مسار التسجيل).
* **الخطر المحتمل:** تخزين كلمات المرور كنصوص واضحة يعني أنه في حال اختراق قاعدة البيانات، سيتمكن المهاجم من سرقة جميع حسابات المستخدمين واستخدامها.
* **مستوى الخطورة:** 🔴 حرج (Critical).
* **طريقة المعالجة:** تم استخدام مكتبة `bcrypt` لتشفير كلمات المرور (Hashing) قبل حفظها في قاعدة البيانات. كما تم التأكد من عدم إرجاع حقل `password_hash` نهائياً في استجابات الـ API (JSON Response).

## 3. غياب التحقق من المدخلات وثغرات XSS (Missing Input Validation & XSS)
* **المكان:** جميع مسارات الـ POST و PUT (مثل `createProduct`, `updateProduct`).
* **الخطر المحتمل:** إدخال بيانات غير منطقية (مثل سعر أو مخزون سالب) يؤدي إلى خلل في النظام المالي للمتجر. كما أن غياب الفحص قد يسمح بإدخال أكواد خبيثة (HTML/JS) داخل حقول الوصف (XSS) لتنفيذها في متصفحات المستخدمين لاحقاً.
* **مستوى الخطورة:** 🟠 مرتفع (High).
* **طريقة المعالجة:** تم استخدام `express-validator` لبناء طبقة حماية (Middleware) تفحص جميع المدخلات وترفض القيم السالبة والفارغة. كما تم استخدام دالة `escape()` لتحويل الرموز الخاصة ومنع هجمات XSS.

## 4. غياب التفويض والوصول غير المصرح به (Broken Access Control & IDOR)
* **المكان:** مسارات إدارة المنتجات (`productsRoutes.js`) واسترجاع بيانات المستخدمين (`getUserById`).
* **الخطر المحتمل:** كان بإمكان أي مستخدم غير مسجل (أو بصلاحية Customer) تعديل أو حذف المنتجات. كما كان بإمكان المستخدم تغيير الـ ID في المسار للوصول إلى بيانات مستخدمين آخرين (Insecure Direct Object Reference - IDOR).
* **مستوى الخطورة:** 🟠 مرتفع (High).
* **طريقة المعالجة:** 
  1. تم إنشاء نظام مصادقة JWT لمنع غير المسجلين.
  2. تم إنشاء Middleware `authorizeRoles` لحصر عمليات الإضافة والتعديل بـ `admin`.
  3. تم تعديل الكنترولر ليقوم بمطابقة `req.user.id` مع الـ ID المطلوب لمنع الوصول لبيانات الآخرين (IDOR Prevention).

## 5. تسريب المعلومات عبر معالجة الأخطاء (Information Exposure via Error Handling)
* **المكان:** التطبيق بشكل عام عند حدوث خطأ غير متوقع (`app.js`).
* **الخطر المحتمل:** ظهور الـ `Stack Trace` أو رسائل الخطأ الخاصة بـ PostgreSQL للمستخدم النهائي يمنح المهاجمين معلومات دقيقة عن هيكلية قاعدة البيانات والأدوات المستخدمة، مما يسهل عليهم بناء هجمات موجهة (مثل SQL Injection).
* **مستوى الخطورة:** 🟡 متوسط (Medium).
* **طريقة المعالجة:** تم بناء `Global Error Handler` يلتقط جميع الأخطاء، يسجلها داخلياً في الخادم باستخدام `console.error` للتشخيص (Secure Logging)، ويعيد للمستخدم النهائي رسالة عامة وآمنة `"Internal server error"` بحالة `500`. كما تم ضبط مسار مخصص للرد بحالة `404` للمسارات غير الموجودة.